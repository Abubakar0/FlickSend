# StreamPack

Transport-independent M6 folder metadata, canonical mapping, and manifest validation. It does not
depend on React, Next.js, WebRTC, authentication, databases, or destination filesystem APIs.
